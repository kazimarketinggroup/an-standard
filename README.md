# an-standard

Minimal Next.js 14 app (App Router) with TypeScript, Tailwind, and ESLint.

Scripts:

- `npm run dev` — start dev server
- `npm run build` — build production
- `npm run start` — start built app
